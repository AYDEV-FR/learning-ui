# Learning UI Container
# Generic web interface - scenarios are mounted via ConfigMap

# Frontend build stage (Vite + React via Bun).
# Runs on the build host's native arch ($BUILDPLATFORM); output is static, arch-independent.
FROM --platform=$BUILDPLATFORM oven/bun:1 AS frontend

WORKDIR /web

# Install deps first for better layer caching
COPY web/package.json web/bun.lockb* ./
RUN bun install --frozen-lockfile

# Build the static assets into /web/dist
COPY web/ ./
RUN bun run build

# Go build stage. Runs natively on $BUILDPLATFORM and cross-compiles to the
# target arch via Go's GOOS/GOARCH — avoids slow/unstable QEMU emulation of Go.
FROM --platform=$BUILDPLATFORM golang:1.22-alpine AS builder

WORKDIR /build

# Install dependencies
RUN apk add --no-cache git

# Copy go mod files
COPY go.mod go.sum* ./
RUN go mod download

# Copy source code and the built frontend (embedded via //go:embed all:web/dist)
COPY main.go ./
COPY --from=frontend /web/dist ./web/dist

# Build the binary for the target platform (set by buildx)
ARG TARGETOS
ARG TARGETARCH
RUN CGO_ENABLED=0 GOOS=${TARGETOS:-linux} GOARCH=${TARGETARCH:-amd64} \
    go build -ldflags="-w -s" -o learning-ui .

# Runtime stage
FROM alpine:3.19

WORKDIR /app

# Install kubectl for executing checks in shell pod
RUN apk add --no-cache ca-certificates curl bash && \
    ARCH=$(uname -m) && \
    if [ "$ARCH" = "x86_64" ]; then ARCH="amd64"; fi && \
    if [ "$ARCH" = "aarch64" ]; then ARCH="arm64"; fi && \
    curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/${ARCH}/kubectl" && \
    chmod +x kubectl && \
    mv kubectl /usr/local/bin/

# Copy binary from builder
COPY --from=builder /build/learning-ui /app/learning-ui

# Create scenarios mount point
RUN mkdir -p /scenarios

# Create non-root user
RUN adduser -D -u 1000 learning && \
    chown -R learning:learning /app /scenarios

USER learning

EXPOSE 8080

ENV PORT=8080
ENV SCENARIO_PATH=/scenarios

CMD ["/app/learning-ui"]
