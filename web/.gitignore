# Frontend dependencies and build output
node_modules/

# Vite build output is embedded into the Go binary at compile time.
# It's generated by `bun run build` (locally) or the Containerfile (CI).
dist/*
!dist/.gitkeep

# Logs / caches
*.log
.vite/
*.tsbuildinfo
